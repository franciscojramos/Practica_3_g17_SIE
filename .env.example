DB_HOST=localhost
DB_PORT=3306
DB_USER=root
# Si MariaDB rechaza la conexión, pon aquí la contraseña del usuario (la misma que uses en mysql -u root -p)
DB_PASSWORD=
DB_NAME=bookar_p1
SESSION_SECRET=cambiar_por_un_secreto_largo_en_produccion
PORT=3000
